# T-VM-002: 集成 TimerEngine 奖励到 ViewModel

**状态：** ✅ done（2026-06-20 19:50 by subagent-α）
**优先级：** P0
**预估：** 2h
**Owner：** subagent-α
**依赖：** T-VM-001, T-CORE-006
**可并行：** -
**实际产出：** GameState.kt +GameViewModel.kt

## 目标

在 ViewModel 每次成功消除时调用 TimerEngine.rewardOnMatch()。

## 来源

- 需求：FR-6.6 / FR-6.7 / FR-6.8 / FR-6.9
- 设计：ADR-004-倒计时奖励.md
- 时序图：02-arch-diagram.md §3.1

## 验收标准

- [x] 在 `processMatches` 中调用 `TimerEngine.rewardOnMatch()`
- [x] matches.isEmpty() 时不调用（FR-6.9）
- [x] 连锁中每步都调用（FR-6.8）
- [x] 触发 UI 显示 +5s 飘字（emit reward event）
- [x] 上限 90s 由 TimerEngine 保证

## 技术要点

- 在 _state 中加 `lastRewardSeconds: Int?` 字段
- UI 监听 lastReward 变化显示飘字
- 单测覆盖 5 个边界（ADR-004 列出）

## 产出物

- 修改 `viewmodel/GameViewModel.kt` ✅ step 7 写入 `lastRewardSeconds`
- 修改 `engine/GameState.kt` ✅ 加 `lastRewardSeconds: Int? = null` 字段