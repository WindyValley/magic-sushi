@rem
@rem gradlew.bat — Windows 占位脚本
@rem Magic Sushi 使用本地 gradle 命令（环境已配置 8.5+），不依赖此脚本
@rem 如需真正的 wrapper，请运行: gradle wrapper --gradle-version 8.5
@rem

@echo.
@echo Magic Sushi: gradlew.bat is a placeholder.
@echo Use: gradle ^<task^>  (requires local gradle 8.5+)
@echo.

exit /b 0
